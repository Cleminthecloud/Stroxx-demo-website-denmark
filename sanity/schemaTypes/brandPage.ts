/* Retired 2026-07-07: /brand is now fully code-owned (app/brand/page.tsx) and
   the "Brand guide (page)" document type was removed from the Studio. This file
   is intentionally empty (kept only because it couldn't be deleted in-session);
   safe to delete from the repo. */
export {};
